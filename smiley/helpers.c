#include "helpers.h"

void colorize(int height, int width, RGBTRIPLE image[height][width])
{
    for (int i = 0; i < height - 1; i++)
    {
        for(int j = 0; j < width - 1; j++)
        {
            if (image[i][j] == 0xFF,0xFF,0xFF)
        }
    }
    // Change all black pixels to a color of your choosing
}
