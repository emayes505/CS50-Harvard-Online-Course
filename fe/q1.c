#include <stdio.h>

int main(void) {
    int ** arr1 = malloc(3 * sizeof(int *));
    for(int i = 0; i < 3 i++){
        arr[i] = malloc(2 * sizeof(int));
        int *arr 2 = malloc(3 * 2 * sizeof(int));
    }
}
