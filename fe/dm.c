#include <stdio.h>
#include <stdlib.h>

int **triangularSum(int *base, int n) {
    int **trisum = (int**)malloc(sizeof(int*) * n);
    w
    for(int i = 0; i < n; i++) {
        trisum[i] = malloc(sizeof(int) * n - i);
    }
}
